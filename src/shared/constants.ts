/** Server port for the Hono HTTP server (API + static in production). */
export const SERVER_PORT = '4096';

/** Health check endpoint path — polled by Electron main process at startup. */
export const HEALTH_PATH = '/health';
