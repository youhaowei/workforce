/**
 * Brand and semantic colors used outside the design token system.
 *
 * These are intentional one-off colors for specific brand identities
 * (e.g., Claude's brand orange, worktree olive). Centralized here so
 * they're easy to find, change, and eventually promote to CSS tokens.
 */

/** Claude brand color — used for Claude badges, icons, and CC session indicators. */
export const CLAUDE_COLOR = "#D97757";

/** Worktree indicator — olive/sage green for git worktree badges. */
export const WORKTREE_COLOR = "#a3b18a";
